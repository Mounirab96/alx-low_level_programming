#include "main.h"
/**
 * _abs - Computes the absolute value of an integer.
 * @n: The integer.
 *
 * Return: Returns to the absolute value.
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
